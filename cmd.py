# git pull --recurse-submodules
# git config submodule.recurse true

# git clone
# git submodule update --init

# synchro d'un tag
# dans le submodule -git checkout tag -git push
# dans le module -git submodule update --remote
